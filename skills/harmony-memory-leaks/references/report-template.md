# Memory investigation report

- Exact flow, iterations, wait condition:
- Project/build/device/system:
- Before/after snapshot and delta artifact paths:
- Suspected type/resource and intended lifetime:
- Retained count/size per cycle:
- Ownership/reference evidence:
- Root retaining edge or cleanup omission:
- Patch:
- Same-flow verification:
- Remaining growth/noise:

Use one of these conclusion labels: `proven leak`, `repeatable retained growth`, `suspected growth`, `bounded cache`, or `no app-owned leak shown`.
